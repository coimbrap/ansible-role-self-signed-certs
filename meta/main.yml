---
galaxy_info:
  role_name: self-signed-certs
  author: Pierre Coimbra
  description: Ansible Role for generating and deploying self-signed certificates
  license: GPLv3
  min_ansible_version: 2.1
  platforms:
  - name: debian
    versions:
    - buster
  galaxy_tags:
    - tls
    - openssl
    - certificates
dependencies:
  - pyOpenSSL
